# リージョン設定UIの方針 — 常設パネルは作らない

2026-07-24 の壁打ちで確定。Logicの「リージョンインスペクタ」（左パネル最上段の数値リスト）に相当するUIをLaLaがどう持つかの設計判断。

## 結論

**リージョン設定の常設パネル（インスペクタ）は作らない。** 各項目を「リージョン自身に痕跡が見える」形に設計することで、数値の表示場所そのものを不要にする。

## 判断の核: 可視性の原則

Logicのリージョンインスペクタに数値表示が必要なのは、パラメータが**非破壊の再生時オフセット**（トランスポーズ+2やクオンタイズ1/16がリージョンの見た目に痕跡を残さない）だから。逆に言うと:

> **痕跡が見えるように作った項目は、表示場所が要らない。**

- 非破壊オフセットにする → どこかに数値表示が必須になる（＝パネルを要求する）
- 見える操作・見える描画にする → パネル不要

Logicの配置（リージョン設定がトラック設定の上に縦積み）への違和感の正体は、スコープの違うものが同じパネルに同居する汎用DAWの構造で、LaLaは「インスペクタ」概念自体を輸入しない。

## 項目ごとの割り当て

| 項目 | 方式 | 見え方 |
|---|---|---|
| ミュート | 実装済み | リージョンの減光表示 |
| フェードイン/アウト | **実装済み**（2026-07-26）: リージョン上辺の左右のドラッグハンドル（**選択中のみ**表示・10×14px） | 斜線を引き、線より上を暗く落とす（Logic流）。斜線は未選択でも描く。数値表示なし（ドラッグ中ポップアップのみ・ms表示、音量ドラッグと同じ流儀） |
| リージョンゲイン（オーディオ専用） | **実装済み**（2026-07-26）: 右クリック →「ゲイン…」→ CallOutBoxのスライダー（±12dB・`GainScale`共通） | 波形の描画振幅をゲイン込みでスケール＋0dB以外のとき右上にdBバッジ |
| トランスポーズ（MIDI） | ピアノロール上の**破壊的操作**（ノートを実際に動かす） | ノート位置に痕跡が残る |
| クオンタイズ（MIDI） | **破壊的操作**（実行時にstartPpqを書き換え、戻すのはundo） | 同上。マウス入力はスナップ済みなので出番はリアルタイム録音導入後 |
| ピッチ補正（ボーカル） | **実装済み**（2026-08-21・`docs/plans/2026-08-20-2244-vocal-pitch-correction.md`）: 非破壊・クリップ単位。右クリック →「ピッチ補正…」で独立ウィンドウ。解析はサイドカー `clip-NNN.<digest>.pitch`、編集状態は project.json v21 | 有効なら `♪` バッジ（移調バッジの左）。メニュー項目に「有効」併記 |
| トランスポーズ（オーディオ） | **実装済み**（2026-08-18）: 非破壊パラメータ（下記）。タイムストレッチ（長さのみ変更・小節数で指定）も同じ吹き出しに同居 | 移調は0以外のときだけリージョン隅に `+2` バッジ。ストレッチは長さが変わる＝痕跡が見えるのでバッジ不要 |

「デフォルトは沈黙、逸脱だけ主張」— 常時ゼロ表示が並ぶパネルより、リージョン自身が状態を語る（ミュート減光と同じ文法）。

## オーディオトランスポーズ・タイムストレッチの設計（2026-08-18 実装済み）

唯一「見えない数値」が残る項目であり、唯一**非破壊にすべき**項目。詳細仕様の真実の源は
`docs/plans/2026-08-18-1028-audio-transpose-stretch.md`。

- **用途**: ① 2本目以降のループ素材をプロジェクトのキー/BPMに合わせる（本命）
  ② **録った素材（ボーカル・ラップ）のキー変え** — 歌いやすいキーを探る ③ チョップ素材のピッチいじり
- **非破壊にする理由**: キー探りは「+1→+2→やっぱり-1」と往復するワークフロー。破壊的上書きは劣化が累積する。劣化回避には「常に元音源からnetの半音数でシフト」が必要で、それは元バッファ＋transpose値の保持＝実質非破壊
- **再生方式**: 値の変更時に裏（`RenderCache` のワーカー）でシフト済みバッファを生成してキャッシュ→再生はキャッシュを読む。オーディオスレッドにDSPコストを載せず、品質重視の遅いアルゴリズムを使える。完了までは古い音のまま鳴らし、完了時に音・長さ・波形が一斉に切り替わる
- **DSPは自作しない**: signalsmith-stretch（MIT・commit SHA pin・**固定シード必須**）を採用。品質が実用に足りなければ SoundTouch（LGPL）へ切り替える
- 単位は半音のみ（ファインチューン/セントは需要が出るまで持たない）
- **タイムストレッチは長さのみ変更**（ピッチと連動するテープ式は持たない）。指定は「小節数」— 元BPMのメタデータを要求せず、グリッドに噛むことが直接保証される。保存値は `stretchRatio`（小節数はUIの入力手段。BPM追従しないので小節数を保存するとBPM変更で意味が変わる）

## 見送り

- **オーディオクオンタイズ（Flex Time / Elastic Audio 相当）**: トランジェント検出＋高品質タイムストレッチの大物2つを背負う機能で、本命用途は生ドラム。リージョン単位のオーディオクオンタイズは引き続き不要。ボーカルの**遅れた1音**のタイミング直しは、ピッチエディタ内のノート横ドラッグ（隣接区間が吸収・クリップ長不変）で扱う（2026-08-21・ピッチ補正 plan で変更）
- **MIDIクオンタイズの非破壊版**（グリッドを変えながら試行錯誤する方式）: 表示場所を要求する。破壊的操作＋undoで十分

## 確定（2026-08-18・旧「未決」2項目）

- **具体操作**: 右クリック →「移調・伸縮…」→ ゲインとは**別項目の専用吹き出し**（移調スライダー＋小節数入力＋倍率併記＋元に戻す）。一時的な吹き出しは**ゲインと移調・伸縮の2つで打ち止め**（常設インスペクタに育てない）。ピッチ補正（2026-08-21）は吹き出しでなく**独立ウィンドウ**（ミキサーと同格の別枠）なのでこの上限には数えない
- **キャッシュの保存方式**: project.json には**値のみ**（`transposeSemitones` / `stretchRatio` / レンダードメイン）。加工済みバッファは**読込時に再生成**する（WAV永続化しない。固定シードで毎回同じ音になる）

## 決まったこと（リージョンゲイン・2026-07-26）

- **操作**: 右クリック →「ゲイン…」→ `CallOutBox` の横スライダー。メニュー項目には0dB以外のとき現在値を併記する
  - メニューへの直接埋め込み（`PopupMenu::addCustomItem`）は見送り: スライダー幅がメニュー内で他項目の文字幅に引き伸ばされて解像度を制御できず、メニューがリージョンを覆って波形の変化が見えない
  - ショートカットは付けていない（右クリックで足りるか使ってから判断する）
- **見え方**: 波形の描画振幅にゲインを掛ける＋0dB以外のとき本体右上にdBバッジ。バッジは表示名（左上）とループハンドル（右下）を避けた位置で、幅56px未満のリージョンでは出さない
- **スケール**: サンプル音源のGAINと共通（`GainScale` = ±12dB・0dB吸着・`GainControls.h` のスライダーと値ラベルを共有）
- **ドラッグ中も音に反映する**: 通常の `pushSnapshot()` は鳴っているMIDIを消音＋再発音させるため、スナップショットに「MIDI構成の世代」を持たせ、オーディオ値だけの差し替えでは据え置く経路（`Project::SnapshotChange::audioValuesOnly`）を用意した。undoも専用種別（`UndoStack::EditKind::clipValue`）で同じ経路を通る

## 決まったこと（フェードイン/アウト・2026-07-26）

- **操作**: リージョン**上辺**の左右のドラッグハンドル（フェード終端に追従）。ヒット領域は **10px幅 × 14px高**（見えている部分は上辺の8px）で、モックを実マウスでドラッグして決めた（合成クリックは座標を正確に狙えるため操作性を検証できない）
  - **表示条件は「選択中のみ」**。ループハンドルと完全に同じ規則で、レーンにホバー検知を新設せずに済み、未選択アイテムを掴もうとして誤ってフェードが伸びる事故も起きない（1クリック目は必ず選択になる）
  - 斜線（フェードの痕跡）は**未選択でも描く**
  - 判定順は フェードハンドル（上辺）→ ループハンドル（下辺）→ 移動。連なり全体の幅が40px未満ならハンドルを出さない
  - **ハンドルが重なったとき**（`fadeIn + fadeOut` が全長に近い）は**クリック位置に近い方・等距離ならフェードイン**。固定順だと片方が永久に掴めなくなる
- **カーブはリニアのみ**（Logicのデフォルト curve=0 と同じ）。Logicがカーブを可変にしている事実自体が「唯一の正解が無い」証拠で、固定するなら標準値が妥当。実装も `addFromWithRamp` が厳密に一致する
- **掛かる範囲は「ループの連なり全体」の両端だけ**（繰り返しの間はシームレス）。各反復に掛けると繰り返すたびに音量が抜けてポンプする。描画・ハンドル位置も連なり基準（本体rectでクリップすると2反復目以降のフェードアウトが音に掛かるのに画面に出ない）
- **スナップしない**（フェードは平滑化であって音楽的位置ではない）。微調整手段も入れない（精度が要るときは⌘→でズーム。上限640px/小節＝3.1ms/pxなら20〜50msが6〜16pxで引ける）
- **エンベロープは閉区間**（`n-1` で割る＝最終サンプルのゲインが厳密に0）。半開区間だと最終サンプルに `1/n` が残り（240サンプルで-47.6dB）**段差が消え切らない**
- モデル層で不変条件を強制（`Clip::clampFades()`。規則は「fadeIn を先に頭打ち → 残りで fadeOut」）。ただし**フェード自身のドラッグはこの関数を直接通さない** — fadeIn 優先なので相手を押しのけてしまう。`clampedFadeIn/clampedFadeOut` で「相手の手前」に止める
- ループのドラッグ中の再クランプは**ドラッグ開始時の元値**を基準にする（現在値からだと縮めすぎて戻したときにフェードが復元されない）

### 段差と角（実測して整理した）

| 現象 | いつ起きるか | 聴こえ方 | 直し方 |
|---|---|---|---|
| **段差**（値の不連続） | リージョン端が波形の途中の値のまま無音に接したとき | はっきり「プチッ」。素材を問わない | フェードを入れる（リニアで十分。数msで消える） |
| **角**（傾きの不連続） | リニアのフェードの開始点・終了点。短いほど強い | ごく僅かな「コツ」。5ms以下＋低音のときだけ | 手で引くフェード長（20ms以上）では**対処不要** |

- 段差は `-6dB/oct`、角は `-12dB/oct` で減衰する＝**桁違いに弱い**。手で引くフェードでリニアを選ぶ判断に影響しない
- **⌘Tで分割しただけでは、どちらも起きない**（左右が隙間なく連続再生されて元の波形を再現する）。片方を動かした瞬間に、露出した端が段差になる
- **クリック除去の自動化（全境界に固定5ms等）は入れない**: ⌘Tで切っただけの本来シームレスな継ぎ目に10msのくぼみを新たに作ってしまい（ロングトーンを切ると漏れる）、既存プロジェクトの出力も全部変わる

### オートメーションを持たない（曲末フェードだけ専用機能で切り出す）

**オートメーションの一般形は作らない。曲末フェードだけを専用の1本として持つ**（2026-07-27 に変更。当初はリージョンフェードで賄う方針だった）。

#### オートメーションを入れない理由

音量の出どころが「フェーダー（手）」と「カーブ（再生ヘッド）」の**2つ**になり、どちらが勝つかを人間が管理する必要が出る。全DAWが **Read / Touch / Latch / Write / Off** のモードをユーザーに選ばせているのはそのため。そして「曲全体を1dB上げたい」の正解はこの5つのどれでもなく「カーブ全体の平行移動」なので、さらに **Trim / Relative** が足される＝**1本のフェーダーの裏に3つの数字**がいる状態になる。

このコストは**恒常的ではなく休眠型**である点が重要。カーブを1本も引いていなければ Read は素のフェーダーと同じに振る舞うので普段は何も見えないが、一度引くと「フェーダーが効かない」が予告なく起き、診断には5モード＋Trimというモデル全体の理解が要る。波及して壊れるものも大きい（データの流れが双方向になる・undoの粒度が別ルールになる・リージョンゲインと役割がかぶる・行の高さが可変になり `row = e.y / trackHeight` 型の計算が33か所ある）。

#### 曲末フェードだけを切り出した理由

当初は「曲末に残るリージョンは通常ビート1本（＋アドリブ程度）なので個別フェードで揃う」と見積もったが、手数が**曲末に残っているトラック数に比例する**点が実運用で問題になった。

専用の1本なら、上記のコストをどれも払わない:

- **フェーダーと値を取り合わない** — マスターフェーダー値 × フェード係数の掛け算なので、フェード区間の中でもフェーダーは普通に効く。5モードも Trim も要らない（モードが必要になるのは「同じ1つの値をカーブと手が奪い合う」ときだけ）
- **1本・カーブ固定・ポイント編集なし** — データモデルもUIも増えない
- **トラックレーンに入らない** — 行の高さ可変の問題が発生しない
- EQ / Comp / Reverb / Delay はこの存在を一切意識しない。順序を意識するのは Limiter だけで、答えは自明（**フェードは Limiter の後**。前に置くとリミッターへの突っ込み量が変わってフェード中に音色まで変わる）

「線（連続変化そのものが効果）」が要る用途は限られており、**段（セクション単位の切り替え）で足りるものは全部リージョン単位の操作で代替できる**というのが整理の軸。

| 用途 | 段 / 線 | LaLaでの扱い |
|---|---|---|
| ボーカルのライド（フレーズ単位の音量調整） | 段 | リージョンゲイン（⌘Tで切って±dB）。痕跡がバッジで見える分こちらが上位互換 |
| セクションごとの音量差 | 段 | 同上 |
| 語尾だけディレイを飛ばす（throw） | 段 | アドリブ用トラックを立てて send を常時深くする |
| 特定区間だけFXを切る | 段 | リージョンの抜き差し |
| **曲末フェード** | 線 | **専用機能**（本節） |
| フィルタースイープ | 線 | サンプル素材で代替（[feature-scope.md](feature-scope.md)） |
| オートパン | 線 | 効果であって解決している課題が無い。ラップが主戦場なら出番が来ない |
| テンポ変化・リタルダンド | 別軸 | BPM曲中固定（別の判断） |

#### 再検討の線引き

**「オートメーションでしかできない用途が3つ溜まったら作る」** を置いている。同じ議論を繰り返さないための線引きで、**現在は0個**（曲末フェードは専用機能で解決したのでカウントから外れた）。

候補として現実的なのは次の2つ。どちらも「線が要る」場面が実際に来てから判断すればよい。

- **throw** — 別トラック分けで代替中。頻度が高いので「トラックを分けるのが面倒」となれば1つ目になり得る
- **フィルタースイープ** — 1.5階（自分でビートを作る層）をどこまでやるか次第

関連: `docs/plans/2026-07-21-region-split.md`（⌘T分割）・`docs/plans/2026-07-23-region-move-and-track-reorder.md`（リージョン移動）・`docs/plans/2026-07-26-region-gain.md`（リージョンゲイン）・`docs/plans/2026-07-26-2242-region-fade.md`（フェード）・`docs/plans/2026-07-27-1316-song-end-fade.md`（曲末フェード）
