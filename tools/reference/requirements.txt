# リファレンス分析パイプラインの依存（Python 3.12 / macOS arm64 で実測した組み合わせ）
# 再構築は ./setup.sh

# ステム分離
torch==2.13.0
torchaudio==2.11.0
demucs==4.1.0

# 音声→MIDI
# 0.4.0 は tensorflow-macos<2.15.1 を要求するが py3.12 用の wheel が存在しないので 0.3.0 で固定
basic-pitch==0.3.0
# basic-pitch 0.3.0 は resampy<0.4.3 を指定するが、0.4.2 は setuptools 81+ で消えた
# pkg_resources に依存していて import できない。0.4.3 で上書きする（動作は実測で確認済み）
resampy==0.4.3

# 基礎分析・可視化
librosa==0.11.0
soundfile==0.14.0
matplotlib==3.11.1
pretty_midi==0.2.11.post0
mido==1.3.3
