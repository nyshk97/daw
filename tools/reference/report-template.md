# <曲名> — 分析レポート

<!--
このファイルはレポートの雛形。節構成の決定とその理由は
docs/design/reference-beat.md 「分析レポートの書き方」を参照。

守ること:
  - 曲の話とツールの話を混ぜない（トリム・信頼度・レビュー依頼は付録へ）
  - 冒頭は客観的事実だけ。「一言でいうと」「結論」から始めない
  - 表は付録の「実測データ」へ。本文には主張の直接の根拠になる小さな表だけ
  - 同じ主張を2回説明しない。2回目以降は結論だけ参照する
  - 見出しは分析項目名でなく結論にする（「グルーヴ」→「グルーヴ — 完全に打ち込み。癖は2つだけ」）
  - 引用ブロック(>) は「作るときにどう使うか」と「深掘りのフック」だけに使う
  - 数値を出したらその値である理由を一言添える（CLAUDE.md の説明作法）

【機】= JSONのキーから機械的に埋まる（各節にキーのパスを書いてある）
【判】= 判断が要る。テンプレート化できない
判定語（「ハネ無し」等）の線引きは末尾の「判定語の閾値」にまとめてある。**勝手に別の線を引かない**。
埋まっている見本: report-example.md（同じディレクトリ）
-->

Prod. <プロデューサー> / <レーベル> / <公開日> 公開 / 分析日 <YYYY-MM-DD> / [原曲](<URL>)

## 基本情報

<!--
【機】各行のキーのパス（analysis/ 配下）:
  尺           basics.duration_sec（秒→M:SS） / groove.n_bars
  BPM          basics.tempo.bpm
               ★ .tempo.bpm_beat_track は使わない（ビートトラッカーの粗い値。この曲では 97.6 に対し 99.4 を返した）
               ★ .tempo.octave_check.half/double はオクターブ検算用。ここの値を採用しない
  拍子         4/4 固定（パイプラインは4/4しか見ていない。3拍子系の曲を入れたら全部壊れる）
  キー         basics.key.top5[0].key ＋ groove.bass.pitch_class_weight で裏取り（下記）
  使う音       groove.bass.pitch_class_weight の上位7音
  ハネ         groove.swing.ratio
  クオンタイズ  groove.drums.mid.dev_ms_by_16th の [0],[4],[8],[12]（＝各拍）の絶対値
  コードループ  topline-6s-piano.loop.most_likely（複数ステムで一致することを確認）
  マスター      overview.json .mean_rms_dbfs / .peak_dbfs

【判】キーの確定手順（top5 をそのまま書かない）:
  1. basics.key.top5 の上位は相対調・近親調が並ぶので、1位をそのまま採らない
  2. groove.bass.pitch_class_weight を降順に並べ、上位7音と8番目の間に明確な差があるか見る
     （見本の曲では 7位 0.045 → 8位 0.003 と桁が違った。差が小さければキー判定を「中」に落とす）
  3. その7音がどのスケールかを書く。長短どちらに寄るかは topline の loop_progression で判断する
-->

| | |
|---|---|
| 尺 | <M:SS> / **<N>小節** |
| BPM | **<bpm>** |
| 拍子 | 4/4 |
| キー | **<X major / Y minor>** — <どちらに寄っているか> |
| 使う音 | **<7音をピアノロール表記(#)で>**。帰ってくる和音は **<コード>** |
| ハネ | <有無>（スウィング比 <r> / 0.500がイーブン・0.667が3連） |
| クオンタイズ | <完全 / 甘い>（グリッドからのズレ ±<n>ms） |
| コードループ | **<n>小節** |
| マスター | 平均RMS <n>dBFS / ピーク <n>dBFS |

### 楽器編成 — <n>系統、同時に鳴るのは最大<n>

<!--
【機】stems-6s.json .stems.<名前> と arrangement.json:
  中身の根拠   .spectral_centroid_hz_median / .stereo_width / .band_balance
               ＋ topline-6s-<stem>.timbre.attack_ms_median / .sustain_ratio_250ms
  出入り       arrangement.sections[].active に現れる/消える小節
  同時数       arrangement.sections[].active の長さの最大値
【判】「エレピ系」「広がり系」等の呼び名。measured な根拠を必ず併記する
      （例: 重心1036Hz・アタック31ms・減衰型 → 叩いて減衰する中低域の鍵盤）
      demucs の分類は実楽器と1対1ではないので、断定せず「〜系」に留める
-->

| 系統 | 中身 | 出入り |
|---|---|---|
| ドラム | | |
| ベース | | |
| 上モノA | | |
| ボーカル | | |

系統の名前は demucs の分類なので、実楽器と1対1ではない。

### 構成 — <n>小節単位、大きく<n>つ

<!-- 【機】arrangement.json .sections[]。3ブロック程度に潰す。細かい表は下の各論へ -->

- **<a>-<b>小節 イントロ**:
- **<a>-<b>小節 本編**:
- **<a>-<b>小節 アウトロ**:

---

## ハーモニー — <結論を見出しに>

<!--
★ まず topline-*.json の chord_estimate_usable を見る。false のステムの進行は読まない。
  上モノがほぼ無い曲では3ステムとも同じかぶりノイズを見て**揃って同じ誤答**を出すので、
  「複数ステムが一致したから信用してよい」が成立しない（実測: 全ステム "Fmaj9"）。
  全ステム false ならコード進行の節は「上モノが無いため推定しない」と書く
【機】topline-6s-piano.json（guitar / other とも突き合わせる）:
  進行         .loop_progression.progression[] の chord / conf / stability
  ループ長      .loop.most_likely と .similarity_by_lag
  折り畳み回数  .loop_progression.repetitions_folded（多いほど誤検出が消える）
  ★ .chords[] は区間ごとの生の推定でノイジー。**進行表には使わない**（畳んだ方を使う）
【判】なぜその響きになるか。7thの有無・動かない小節・ルートの方向・緊張と緩和。
      確信度の低いスロットは表に「確信度低め」と明記し、耳での確認項目に回す
-->

## グルーヴ — <結論を見出しに>

<!--
【機】groove.json:
  16分プロファイル  .drums.{low,mid,high}.profile_by_16th（low=キック / mid=スネア・クラップ / high=ハット）
  マイクロタイミング .drums.*.dev_ms_by_16th（+ = もたつく / - = 食う）
  スウィング         .swing.ratio / .offbeat_lag_ms
  ベース             .bass.notes_per_bar / .pitch_p5 / .pitch_p95 / .dur_median_in_16ths / .onset_rate_by_16th
  ★ .drums.low の16分位置は信用しない（時間分解能不足）。拍単位に丸めて読む
  ★ .drums.high にはクラップのアタックが混入する
【判】ここが本体。数字を「定石」と「この曲の癖」に切り分ける。曲の個性は大抵「癖」側にある
-->

**定石通りのところ**

**この曲の癖**

## 音色と帯域 — <結論を見出しに>

<!--
【機】stems-6s.json .stems.*.band_balance / .spectral_centroid_hz_median / .stereo_width
      topline-6s-<stem>.json .timbre.attack_ms_median / .sustain_ratio_250ms / .flatness_median
  ★ 重心は **_median** を使う。**_mean は「その楽器が鳴っていないフレーム」（無音・減衰の尻尾＝
    広帯域ノイズ）に引っ張られて高く出る。piano で中央値1036Hz に対し平均1480Hz（44%ずれ）
  ★ 1つの表に4分割と6分割の値を混ぜない。混ぜると同じ "other" が別物になる
    （4分割の other は「ボーカル・ドラム・ベース以外の全部」、6分割の other は piano/guitar を抜いた残り）
【判】その数値が聴こえ方にどう効くか。帯域の住み分け・前後感・音の混み具合
-->

## 構成 — <結論を見出しに>

<!-- 【機】arrangement.json .sections[]（bar_start / bar_end / active / rms_db_mean）
     【判】何を抜き差ししてエネルギー曲線を作っているか -->

## 「<本人が使った言葉>」は何でできているか

<!-- 【判】各論の総合。本人がその曲をどう呼んでいるか（「メロディアスなやつ」等）を
     測った要素に分解し、効きが大きい順に並べる。最後に「逆に言うと」で
     作るときの優先順位に落とす。各論で説明済みの事実は繰り返さず結論だけ参照する -->

---
---

# 付録: この分析について

以下は曲の話ではなく、上の数字がどう出たか・どこまで信じてよいかの話。

## 分析の条件

<!--
【機】overview.json .near_silence がトリム位置の候補。
      曲頭側の最後のギャップの終わり 〜 曲尾側の最初のギャップの始まり、が本編の目安
【判】そのギャップが本当にMVエディットの切れ目か（曲中のブレイクを誤って切らないこと）。
      どこを切ったかで全部の小節番号が変わるので必ず書く
-->

数値の出どころは [analysis/](analysis/) 配下の JSON。手順は `tools/reference/analyze.sh`。

## 実測データ

<!-- 【機】本文から追い出した表を全部ここへ。読むものでなく引くもの -->

**16分ごとの出現強度**（各帯域の最大値を100とした相対値。図: [analysis/groove.png](analysis/groove.png)）

**ステムごとの帯域と質感**（[analysis/stems-6s.json](analysis/stems-6s.json)）

**ループ長の判定**

## どこまで信じてよいか

<!--
【機】下記のうち「根拠」列がツールの性質の行は毎回同じ。上4行だけ曲ごとに埋める:
  BPM            basics.tempo.grid_contrast_by_eighth が全区間 1.0 超で後半に急落しないか
  小節頭          groove.drums.mid.profile_by_16th の [4] と [12] が突出しているか（2拍・4拍のバックビート）
  キー            上の「キーの確定手順」の 7位と8位の差
  コード進行の骨格  topline-6s-{piano,guitar,other} の loop_progression が一致しているか
-->

| 項目 | 信頼度 | 根拠 |
|---|---|---|
| BPM | | |
| 小節頭の位置 | | |
| キー | | |
| コード進行の骨格 | | |
| コードの細かい種別（9th/sus/転回） | **中** | chroma のにじみで隣接音の区別が甘い |
| クラップ/ハットの16分位置 | **中〜高** | 中高域はアタックが立つので位置は取れる。ただしハット帯域にクラップのアタックが混入する |
| キックの16分位置 | **低** | 低域は時間分解能が原理的に足りず1〜2個ぶん滲む。拍単位でしか読んでいない |
| ベースの音高 | **中** | pyin がオクターブを取り違えるので 5-95% 区間で見ている |
| 音声→MIDI（basic-pitch） | **低〜中** | 持続音を短い音に刻む。統計を借りる用途には足りるが譜面としては使えない |

## 耳で検算する

[listen/](listen/) に確認用の短いクリップを、確認する順に番号を振って切り出してある。
Finder で開いてファイルを選び**スペースキー**（Quick Look）を連打すれば一周できる。

| クリップ | 何を判定するか |
|---|---|
| `01-click-head` / `01-click-tail` | **BPM と小節頭**。曲頭と曲尾の両方でズレなければ正しい |
| `02-chords-check` | **コード進行**。推定した和音をサイン波で重ねてある。合っていれば曲に溶け、違えば濁る |
| `03-loop-only` | 02 と同じ区間のコード無し版。聴き比べ用 |
| `04-stem-*-loud` | 各ステムの素性（一番大きい区間） |
| `04-stem-*-quiet` | **分離の破綻・幽霊音**（鳴っているが小さい区間） |
| `05-midi-*` | **basic-pitch の精度**。左が原音、右がMIDIをサイン波で |

メモしてほしいのは2つ:

1. 各クリップが OK か NG か（NG ならどう聴こえたか一言）
2. **「特にここが好き」な瞬間を2〜3個**。次の曲の分析でそこを重点的に見る

<!--
================================================================
判定語の閾値 — 曲をまたいで目盛りを揃えるための取り決め

数値をそのまま書くだけなら誰がやっても同じだが、「ハネ無し」「完全にクオンタイズ」の
ような判定語は線引き次第で変わる。ライブラリの横断レビューが目的なので、
**曲ごとに線を引き直さない**。線を変えるときは、この表ごと変えて理由を labs に残す。
（★印は 2026-08-03 に第1号を書いたとき仮に決めたもの。2曲目以降で見直してよい）

■ ハネ  groove.swing.ratio
    < 0.52        無し（イーブン）★
    0.52 - 0.58   軽くハネる ★
    > 0.58        はっきりハネる（0.667 が3連）★
    ※ .swing.plausible が false → 測定が破綻している。「ハネ無し」と書かず「測れなかった」と書く
      （ハットの無い曲で高域が別の音を拾うと 0.5 未満になる。実測 0.385 = 裏拍が94ms"早い"）
    ※ .swing が無い（null）＝高域の裏拍が足りず測れなかった。これも「無し」ではない

■ クオンタイズ  groove.drums.mid.dev_ms_by_16th の [0][4][8][12] の絶対値
    全て ≤ 10ms   完全（打ち込み）★
    ≤ 25ms        甘い（人力 or 意図的なヨレ）★
    > 25ms        生演奏、またはグリッドがずれている（先にグリッドを疑う）★

■ テンポ固定か  basics.tempo.grid_contrast_by_eighth（8区間）
    全区間 > 1.0 かつ 後半で急落しない   → 固定テンポ。BPM を断定してよい
    後半だけ落ちる                      → 走る/もたつく曲。剛体グリッドが破綻している
    ※ basics.tempo.local_bpm_std < 1.0 も固定テンポの傍証

■ コードループ長  topline-*.loop.similarity_by_lag
    採用値が他より +0.05 以上高い       → 断定してよい
    差が 0.05 未満                     → 「〜小節（確信度低め）」と書く
    ※ 複数ステムで一致しないループ長は採用しない

■ コード1スロットの確信度  loop_progression.progression[].conf
    同ループ内の最大 conf から -0.2 以内  → 断定
    -0.2 より低い                        → 「確信度低め」と明記し、耳での確認項目に回す
    ※ stability は「毎回同じ響きか」で、conf とは別物。両方見る

■ ステム分離  stems-*.reconstruction.residual_below_mix_db
    ≤ -20dB       使える ★
    -20 〜 -15dB  条件つき（分析には使えるが音として使うのは避ける）★
    > -15dB       疑う ★
    ※ 併せて stems.*.floor_below_peak_db（休符区間の残留）を見る。
      ただし曲中ずっと鳴っているステム（drums 等）では意味を持たない

■ basic-pitch  topline-*.midi.chord_tone_ratio / .grid_dev_ms_within_30ms
    chord_tone_ratio ≥ 0.8 かつ within_30ms ≥ 0.7  → 統計を借りる用途には足りる ★
    それ未満                                        → 統計も疑う
    ※ ボーカルの within_30ms が低いのは正常（歌はグリッドに乗らない）
================================================================
-->
